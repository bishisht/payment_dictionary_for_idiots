% preamble.tex - Package Configuration and Setup

% Engine-aware font setup
\usepackage{iftex}
\ifPDFTeX
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{courier}
\else
\usepackage{fontspec}
\defaultfontfeatures{Ligatures=TeX}
\setmainfont{Times New Roman}
\setmonofont{Courier New}
\fi

% Page geometry - sensible margins
\usepackage[margin=2.5cm]{geometry}

% Dictionary-style two-column tuning
\setlength{\columnsep}{18pt}
\setlength{\columnseprule}{0pt}

% Index generation
\usepackage{imakeidx}
\makeindex

% Bibliography management
\usepackage[backend=biber,style=alphabetic]{biblatex}
\addbibresource{backmatter/references.bib}

% Hyperlinks (load near end to avoid conflicts)
\usepackage{hyperref}
\hypersetup{
    pdftitle={Payment Dictionary for Idiots},
    pdfauthor={Bishisht Bhatta},
    pdfsubject={A Learning Resource for Payment Industry Terminology},
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    urlcolor=blue,
    bookmarksnumbered=true,
    pdfstartview=FitH
}

% Better definition lists
\usepackage{enumitem}
\usepackage{ragged2e}

% Special symbols
\usepackage{textcomp}

% Better typography
\usepackage{microtype}

% Custom dictionary styling
\usepackage{styles/dictionary_style}
